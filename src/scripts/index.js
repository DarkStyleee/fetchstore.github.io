import initPopups from './parts/popups';
import initSliders from './parts/sliders';
import initForm from './parts/form-success';

$(document).ready(() => {
  initPopups();
  initSliders();
  initForm();
});